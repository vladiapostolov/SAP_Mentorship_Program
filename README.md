Scope of the project:
- Create a mobile app that scans the QR via the camera or other methods
- The app will have authentication/verification of the user!
- The app will have some kind of frontend(we have to decide what kind, AI assistance)
- Backend- Python/Java/Jython(? :D)
- DB- Postgres
- API's that would be useful- OpenCV(find out how it works)?

moew